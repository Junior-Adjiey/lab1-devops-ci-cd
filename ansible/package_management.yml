---
- name: Package management
  hosts: docker
  become: yes

  tasks:

    - name: Install htop
      apt:
        name: htop
        state: present
      tags:
        - install

    - name: Remove htop
      apt:
        name: htop
        state: absent
      tags:
        - remove